import Router from 'components/Router/Router';

const App = () => {
  // const [isLoading, setIsLoading] = useState(false);

  // useEffect(() => {
  //   // 기본으로 로딩 0.2초는 기다림
  //   setTimeout(() => {
  //     setIsLoading(true);
  //   }, 200);
  // }, []);

  return <Router />;
};

export default App;
